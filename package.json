{
  "scripts": {
    "check:syntax": "node --check tools/import-promos.mjs && node --check assets/app.js && node --check tools/validate-data.mjs",
    "check:data": "node tools/validate-data.mjs",
    "check": "npm run check:syntax && npm run check:data"
  }
}
